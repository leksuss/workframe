# Example: Django App

This is an adaptation note, not a payload to copy blindly.

Suggested modules:

- `template/base`
- `template/modules/codex-skills`

Optional modules:

- `template/modules/frontend-quality` if the project has templates or frontend UI.
- `template/modules/design-pencil` if visual design artifacts are part of the workflow.
