# Example: Telegram Bot

This is an adaptation note, not a payload to copy blindly.

Suggested modules:

- `template/base`
- `template/modules/codex-skills`

Focus `docs/CONCEPTS.md` on user journeys, command flows, operational constraints, and what the bot should not automate.
