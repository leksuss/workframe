# Cursor Adapter

Cursor reads root `AGENTS.md` as project instructions, so Workframe needs no duplicate Cursor Rule. Cursor users receive the common always-on rules; OpenSpec workflows can be invoked by asking the agent to read the matching `.agents/skills/` file.

Reference: https://docs.cursor.com/context/rules-for-ai
