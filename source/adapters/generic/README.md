# Generic Agent Adapter

For AI tools that do not support local skills, use:

- `AGENTS.md` for operating rules.
- `docs/AGENT_WORKFLOW.md` for neutral process guidance.
- `docs/checklists/` for repeatable verification.

If the tool supports its own instruction filename, adapt `template/base/AGENTS.md` into that format without changing the underlying workflow.
