# Telegram Bot Profile

Use this profile when adapting Workframe to a Telegram bot.

Typical extra checks:

- command and callback flows are documented;
- error and retry behavior is clear;
- message text is reviewed as product UI;
- rate limits and idempotency are considered;
- secrets and bot tokens are never committed;
- integration behavior is represented in OpenSpec when user-visible.
