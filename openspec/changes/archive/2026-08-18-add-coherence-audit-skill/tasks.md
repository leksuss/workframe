## Фаза 1. Каркас

## 1. Канонический workflow

- [x] 1.1 Создать `template/modules/agent-skills/.agents/skills/coherence-audit/SKILL.md` с frontmatter в стиле существующих канонических скиллов и процедурой аудита: подготовка change и ветки, чтение `docs/checklists/coherence-audit.md` как первый обязательный шаг, порядок срезов с обязательностью первых трёх, обработка находок по классам, обновление и ре-валидация реестра, условия остановки, формат итогового отчёта владельцу. Ограничение: файл не повторяет содержание семи срезов и не переопределяет классификацию находок из `AGENTS.md`. Проверка: ни одно название среза не раскрывается в skill содержательно, а каждый шаг процедуры отсутствует в чек-листе, то есть добавляет к нему, а не дублирует его.
- [x] 1.2 Создать discovery adapters `coherence-audit/SKILL.md` в `.codex/skills/`, `.claude/skills/` и `.qwen/skills/` модуля `agent-skills` по образцу существующих. Ограничение: каждый adapter содержит только frontmatter и указание прочитать канонический файл, объём сопоставим с существующими шестью строками. Проверка: каждый adapter ссылается на `.agents/skills/coherence-audit/SKILL.md` и не содержит текста процедуры.

## 2. Проверка payload

- [x] 2.1 Добавить `coherence-audit` в массив `SKILLS` в `scripts/verify-agent-adapters.sh` и заменить проверку дублирования инструкций на проверку объёма adapter в сочетании с обязательной ссылкой на канонический файл. Ограничение: разрешение соответствует варианту (c) записи `D-001`; проверка не должна зависеть от текста описания отдельного skill. Проверка: `bash scripts/verify-agent-adapters.sh` завершается с кодом 0, а негативные контроли дают ненулевой код — сломанная ссылка в adapter, удалённый adapter и вклейка полного канонического текста в adapter, причём последний контроль воспроизводится и для `openspec-explore`, на котором прежняя проверка не срабатывала.
- [x] 2.2 Дополнить smoke-часть скрипта проверкой того, что новый skill и его адаптеры попадают в инициализированный проект. Проверка: временная инициализация содержит `.agents/skills/coherence-audit/SKILL.md` и три клиентских адаптера.

## 3. Согласование формулировок

- [x] 3.1 Уточнить `template/modules/agent-skills/README.md` и `template/base/CLAUDE.md`: канонический каталог содержит рабочие процессы проекта, а не исключительно OpenSpec workflows. Ограничение: поведение не меняется, правка только текстовая. Проверка: ни один файл payload не утверждает, что `.agents/skills/` или `.claude/skills/` содержат только OpenSpec workflows.

- [x] 3.2 Дополнить `docs/UPGRADING.md` файлами нового skill в списке сравнения и добавить запись в `CHANGELOG.md`. Ограничение: не исправлять в этом change заголовок списка, ошибочно относящий payload опциональных модулей к устанавливаемому в каждый проект, — это отдельная находка для аудита. Проверка: список содержит canonical и клиентские пути `coherence-audit`, а CHANGELOG описывает появление workflow и починку проверки адаптеров.

## 4. Реестр и сверка

- [x] 4.1 Перевести запись `D-001` в `docs/DEBT.md` в статус `resolved` с указанием change-id и выбранного разрешения. Ограничение: запись не удаляется. Проверка: запись сохранена, статус изменён, названо разрешение (c) и причина отклонения вариантов (a) и (b).
- [x] 4.2 Сверить реализацию с `proposal.md`, `design.md` и delta specs, отметить выполненные пункты и выполнить сверку перед archive по разделу `Coherence` в `AGENTS.md`. Проверка: `openspec status --change add-coherence-audit-skill` показывает готовые artifacts, в добавленных файлах нет placeholder, а ссылки из новых файлов разрешаются.

## Фаза 2. Углубление

Пусто. Первый прогон аудита описан в `proposal.md` как отдельный change `coherence-audit-2026-08`.
