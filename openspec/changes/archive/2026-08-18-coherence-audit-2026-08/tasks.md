## Фаза 1. Каркас

## 1. Исправление находок mechanical

- [x] 1.1 Заполнить `Purpose` в `openspec/specs/agent-sequencing/spec.md` и `openspec/specs/workframe-governance/spec.md` по фактическому содержанию их требований. Ограничение: Purpose описывает назначение capability и не перечисляет требования, чтобы не устаревать при добавлении отдельного требования. Проверка: ни в одной спеке не остаётся строки `TBD`, `openspec validate --specs` проходит.
- [x] 1.2 Разделить в `docs/UPGRADING.md` список файлов на устанавливаемые в каждый проект и приходящие с опциональными модулями, добавить пропущенную строку `.codex/skills/openspec-*`. Ограничение: состав перечисленных файлов не меняется, меняется только группировка и полнота. Проверка: под заголовком об устанавливаемом всегда нет файлов `design-pencil` и `frontend-quality`, а адаптеры Codex присутствуют.
- [x] 1.3 Добавить в `template/base/docs/checklists/coherence-audit.md` исключение для намеренно незаполненной quality policy. Ограничение: исключение названо точечно и не ослабляет срез для прочих placeholder. Проверка: чек-лист явно называет `pending stack selection` и строку-образец таблицы ожидаемым состоянием до выбора стека.
- [x] 1.4 Записать в дельту `coherence-lifecycle` требование о том, что намеренно предписанное состояние не является находкой. Ограничение: формулировка ограничена состояниями, требуемыми действующей спекой или policy, и не даёт общего права игнорировать placeholder. Проверка: `openspec validate --changes` проходит, а сценарий описывает и правило, и обязанность чек-листа называть такое состояние явно.
- [x] 1.5 Закрыть дыру, обнаруженную самим прогоном: skill требует цитировать места находки, но не требует проверять, что цитаты разрешаются. Добавить требование в дельту `coherence-lifecycle`, шаг в канонический skill и пункт в чек-лист. Ограничение: правило говорит о проверке ссылок записи, а не о повторном прохождении срезов. Проверка: все три файла называют смещение номеров строк при `mechanical`-починке как причину, а ссылки записей `D-002`–`D-006` перепроверены по этому правилу.

## 2. Фиксация находок semantic и structural

- [x] 2.1 Записать в `docs/DEBT.md` находки `D-002`–`D-005` с обеими сторонами расхождения и вариантами разрешения. Ограничение: ни одна из них не исправляется в этом change. Проверка: каждая запись содержит проверяемые места и воспроизведённое доказательство там, где оно получено.
- [x] 2.2 Записать `D-006`: схема `spec-driven` отвергает change без дельты, из-за чего аудит, нашедший только `mechanical`-находки, не может быть оформлен предписанным способом. Ограничение: выбор политики за владельцем, в этом change не решается. Проверка: запись содержит воспроизведённое сообщение валидатора и три варианта разрешения.

## 3. Сверка change

- [x] 3.1 Выполнить сверку перед archive по разделу `Coherence` в `AGENTS.md` и отметить выполненные пункты. Проверка: `openspec validate` проходит, `bash scripts/verify-agent-adapters.sh` завершается кодом 0, в изменённых файлах нет незаполненных placeholder.

## Фаза 2. Углубление

Пусто. Все отложенные находки записаны в `docs/DEBT.md`, а не в этот раздел: он архивируется вместе с change.
