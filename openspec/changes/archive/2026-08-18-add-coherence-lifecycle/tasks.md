## Фаза 1. Каркас

## 1. Нейтральный канон

- [x] 1.1 Создать `source/canonical-rules/coherence.md` с иерархией истины, read-only статусом архива, тремя классами находок, двумя уровнями срабатывания и правилами реестра. Ограничение: файл нейтрален к AI-клиенту и стеку, не называет инструментов и не дублирует текст `workflow.md`. Проверка: каждое решение из `design.md` разделов 1–6 имеет соответствующий абзац канона.
- [x] 1.2 Обновить `source/canonical-rules/workflow.md`: секция `Sequencing` должна называть постоянный адрес бэклога вместо безадресного «a backlog», секция `Verification` — ссылаться на `coherence.md` как на смежный lifecycle. Проверка: в каноне не остаётся упоминаний бэклога без указания места хранения.

## 2. Правила и реестр в payload

- [x] 2.1 Добавить раздел `## Coherence` в `template/base/AGENTS.md`: иерархия истины, архив только для чтения, классы находок, обязательная сверка перед archive, обращение к реестру при планировании change. Ограничение: раздел следует стилю файла — короткие утверждения и списки, без повторения текста `AGENT_WORKFLOW.md`. Проверка: раздел покрывает все четыре проверки сверки из `design.md` решения 4.
- [x] 2.2 Обновить в `template/base/AGENTS.md` разделы `Feature Workflow` и `Work Sequencing`: сверка становится условием предложения archive, а невыполненные пункты `## Фаза 2. Углубление` переносятся в реестр до архивации. Проверка: строка о `## Фаза 2. Углубление` больше не оставляет отложенную работу единственно внутри `tasks.md`.
- [x] 2.3 Создать `template/base/docs/DEBT.md` — пустой реестр с шапкой правил ведения, описанием статусов и одним закомментированным или явно помеченным примером записи. Ограничение: файл не содержит выдуманных находок и не требует заполнения при инициализации. Проверка: новый проект получает файл, который читается как инструкция и не содержит фиктивных данных.
- [x] 2.4 Добавить в `template/base/docs/AGENT_WORKFLOW.md` раздел о двух уровнях сверки и жизненном цикле реестра: пополнение, расходование при планировании, ре-валидация, принудительный вынос залежавшихся записей. Проверка: описание согласуется с `AGENTS.md` и не противоречит ему в области сверки.

## 3. Чек-листы

- [x] 3.1 Создать `template/base/docs/checklists/coherence-audit.md` с семью срезами в порядке от объективных к субъективным, правилом обязательности срезов 1–3 и требованием дословной цитаты обеих сторон для каждой `semantic`-находки. Проверка: чек-лист выполним агентом без поддержки skills и не ссылается на несуществующий skill.
- [x] 3.2 Дополнить `template/base/docs/checklists/release-readiness.md` пунктами сверки перед archive. Ограничение: не дублировать существующие пункты о соответствии реализации активному change. Проверка: чек-лист содержит пункт об отсутствии placeholder и пункт о переносе отложенных улучшений в реестр.

## 4. Governance самого Workframe и документация

- [x] 4.1 Добавить раздел `## Coherence` в root `AGENTS.md`, адаптированный под границу root и `template/`, и создать root `docs/DEBT.md` как реестр самого Workframe. Ограничение: root-версия не описывает payload-специфичные пути как собственные; root-реестр не дублирует правила, а ссылается на них. Проверка: правила применимы к работе над Workframe, разделение root и template сохранено согласно capability `workframe-governance`, а каждая ссылка на реестр из root `AGENTS.md` разрешается в существующий файл.
- [x] 4.2 Обновить `README.md`, `README.ru.md`, `docs/UPGRADING.md` и `CHANGELOG.md`: строка о `docs/DEBT.md` в таблице payload, порядок принятия lifecycle существующим проектом, запрет на перезапись чужого реестра шаблоном, запись об изменении. Проверка: обе языковые версии README содержат одинаковые факты, а `docs/UPGRADING.md` описывает принятие как отдельный change целевого проекта.

## 5. Сверка change

- [x] 5.1 Сверить реализацию с `proposal.md`, `design.md` и delta specs, отметить выполненные пункты и применить к самому change правила сверки из пункта 2.1. Проверка: `openspec status --change add-coherence-lifecycle` показывает готовые artifacts, в добавленных файлах нет placeholder, а все контракты задач подтверждены фактическим содержимым файлов.
- [x] 5.2 Заменить в `scripts/verify-agent-adapters.sh` вызовы `rg -F` на `grep -qF`. Обоснование: ripgrep не установлен у владельца, из-за чего единственная проверка репозитория не запускалась; скрипт не использует ни одной возможности ripgrep помимо поиска фиксированной строки. Ограничение: поведение проверок не меняется, вторая проверка не переписывается в этом change. Проверка: `bash scripts/verify-agent-adapters.sh` завершается с кодом 0, а негативные контроли — сломанная ссылка в adapter и удалённый adapter — дают ненулевой код.
- [x] 5.3 Зафиксировать в `docs/DEBT.md` находку о нерабочей проверке дублирования инструкций для трёх скиллов. Ограничение: находка класса `semantic` не исправляется в этом change. Проверка: запись `D-001` содержит обе стороны расхождения с путями и воспроизведённое доказательство.

## Фаза 2. Углубление

Пусто. Продолжение работы описано в `proposal.md` как два отдельных change (`add-coherence-audit-skill`, `coherence-audit-2026-08`), а не как отложенные улучшения этого change: запланированный change не является ни дефектом, ни техдолгом и не подлежит переносу в реестр.
