## Фаза 1. Каркас

## 1. Payload генерируемого проекта

- [x] 1.1 Заменить в `scripts/init-project.sh` копирование модуля `agent-skills` целиком на перечисление его каталогов payload `.agents`, `.claude`, `.codex`, `.qwen`. Ограничение: не удалять файлы в целевом каталоге и не вводить новых зависимостей. Проверка: инициализация в пустой временный каталог не создаёт `README.md`, а все пять скиллов присутствуют в каноническом и трёх клиентских каталогах.
- [x] 1.2 Исправить команду ручного копирования в `template/modules/agent-skills/README.md`, содержавшую тот же дефект. Проверка: выполнение приведённой команды даёт тот же состав файлов, что и скрипт, и не создаёт `README.md` в проекте.

## 2. Единый каталог материалов адаптации

- [x] 2.1 Удалить `source/profiles/` и обновить упоминания профилей в `AGENTS.md`, `docs/CONCEPTS.md` и `docs/UPGRADING.md` так, чтобы они указывали на `examples/`. Ограничение: `CHANGELOG.md` не изменяется, его запись относится к выпущенной версии; правка `docs/CONCEPTS.md` ограничена заменой исчезнувшей сущности и санкционирована решением по `D-003`. Проверка: во всём репозитории вне архива и `CHANGELOG.md` не остаётся ссылок на профили как на отдельную от `examples/` сущность.

## 3. Реестр и сверка

- [x] 3.1 Перевести `D-002` и `D-003` в `docs/DEBT.md` в статус `resolved` с указанием change-id и выбранного разрешения, а `D-005` — в `rejected` по решению владельца. Ограничение: `D-005` не требует правок кода, фиксируется только решение и его обоснование. Проверка: записи сохранены, статусы изменены, названы применённые варианты и причина отклонения.
- [x] 3.2 Выполнить сверку перед archive по разделу `Coherence` в `AGENTS.md`. Проверка: `openspec validate` проходит, `bash scripts/verify-agent-adapters.sh` завершается кодом 0, ссылки изменённых файлов разрешаются.

## Фаза 2. Углубление

Пусто.
