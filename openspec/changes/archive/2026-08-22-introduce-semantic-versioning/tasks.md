## 1. Контракт версии и выпуск 0.2.0

- [x] 1.1 Добавить root `VERSION` со значением `0.2.0`, перенести накопленные записи из `CHANGELOG.md` `Unreleased` в датированную секцию и указать правила SemVer в root governance. Результат: один канонический номер и зафиксированный первый выпуск. Проверка: `VERSION` валиден, `Unreleased` пуст, changelog содержит `0.2.0`.
- [x] 1.2 Обновить root specs и owner-документацию, чтобы завершение каждого нетривиального Workframe change требовало обоснованного повышения SemVer до archive. Результат: процесс не зависит от неявного знания. Проверка: `openspec validate introduce-semantic-versioning --strict`.

## 2. Версия в создаваемом и обновляемом проекте

- [x] 2.1 Изменить template marker и `scripts/init-project.sh`, чтобы штатная инициализация читала root `VERSION`, записывала его в `.project-workframe-version` и не оставляла backup-файл. Результат: новый проект содержит фактическую версию и дату. Проверка: smoke-copy в temporary directory и проверка marker.
- [x] 2.2 Обновить README и `docs/UPGRADING.md`, описав сравнение с `VERSION`, SemVer и безопасное проектное обновление без автоматической перезаписи. Результат: владелец может поручить обновление агенту. Проверка: ссылки и примеры согласованы с payload.

## 3. Проверка согласованности

- [x] 3.1 Выполнить syntax/smoke проверки изменённого init script, OpenSpec validation и reconcile затронутых артефактов. Результат: механических расхождений и placeholders нет. Проверка: команды завершаются успешно, а результаты отражены в change.
