## Фаза 1. Каркас

## 1. Конвенция дат архива

- [x] 1.1 Записать в `source/canonical-rules/coherence.md`, root `AGENTS.md` и `template/base/AGENTS.md`, что даты каталогов архива трактуются как UTC. Ограничение: правило ставится рядом с порядком архивации, а не в отдельный документ; существующие каталоги архива не переименовываются. Проверка: в обоих `AGENTS.md` порядок архивации называет шкалу дат явно, а канон содержит то же утверждение в нейтральной форме.

## 2. Оформление аудита без изменения требований

- [x] 2.1 Записать в `source/canonical-rules/coherence.md`, оба `AGENTS.md` и `template/base/docs/AGENT_WORKFLOW.md`, что аудит, не изменивший требований, оформляется прямой правкой как purely internal cleanup, оставаясь на отдельной ветке с записями в реестре. Ограничение: критерий тот же, что уже применяется ко всем изменениям — затронуто ли документированное поведение. Проверка: ни один документ не предписывает оформлять любой аудит через OpenSpec безусловно.
- [x] 2.2 Отразить то же в `template/base/docs/checklists/coherence-audit.md` и каноническом skill `coherence-audit`, где подготовка change описана как обязательный шаг. Ограничение: skill остаётся источником процедуры и не начинает дублировать содержание срезов. Проверка: шаг подготовки в skill различает два случая, а чек-лист называет их в разделе подготовки.

## 3. Реестр и сверка

- [x] 3.1 Перевести `D-004` и `D-006` в `docs/DEBT.md` в статус `resolved` с указанием change-id и выбранного разрешения. Проверка: записи сохранены, статусы изменены, названы применённые варианты и причина отклонения альтернатив.
- [x] 3.2 Выполнить сверку перед archive по разделу `Coherence` в `AGENTS.md`. Проверка: `openspec validate` проходит, `bash scripts/verify-agent-adapters.sh` завершается кодом 0, в реестре не остаётся открытых записей без решения владельца.

## Фаза 2. Углубление

Пусто.
