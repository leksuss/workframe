# Project Instructions

Read `AGENTS.md` and `docs/AGENT_WORKFLOW.md` before non-trivial work. They are the canonical always-on rules for this project.

When a project workflow is relevant — an OpenSpec workflow, or the coherence audit — use the matching project skill in `.claude/skills/`; it will load the canonical instructions from `.agents/skills/`.
